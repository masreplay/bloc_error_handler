name: bloc_error_handler
description: handle http client errors using bloc
version: 0.0.1
homepage: https://www.example.com

publish_to: "none"

environment:
  sdk: ">=2.18.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  error_handler: ^1.1.0
  flutter_bloc: ^8.0.1
  chopper: ^4.0.1

dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0
